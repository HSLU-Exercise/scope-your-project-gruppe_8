# So sollten wir immer mit Secrets umgehen!

# 1. Wir importieren die benötigten Werkzeuge
import os  # Für den Zugriff auf System-Funktionen
from dotenv import load_dotenv  # Speziell für .env Dateien

# 2. Wir laden die .env Datei
load_dotenv()  # Diese Funktion sucht nach einer .env Datei und lädt ihre Inhalte

# 3. Wir holen den Stripe-Key aus der .env Datei
stripe_key = os.getenv('STRIPE_KEY')  # Sucht nach "STRIPE_KEY" in der .env Datei

# 4. Wir zeigen nur einen Teil des Keys an - für Sicherheit!
print(f"Zahlung mit Key: {stripe_key[:10]}...")  
# Die [:10] zeigt nur die ersten 10 Zeichen an, nicht den ganzen Key!
